#pragma once
#include <cstddef>

#include "tape/itape.h"

/*
class KWayTapeSorter {
private:
    ITape unsorted_tape_;
    ITape out_tape_;
    size_t buffer_size_;

public:
    KWayTapeSorter(ITape& unsorted_tape, ITape& out_tape, size_t buffer_size);
    void Sort();
}
*/
